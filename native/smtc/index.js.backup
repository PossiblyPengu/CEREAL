// Mock SMTC implementation for testing without native build
// Replace index.js to use this mock for UI testing

module.exports = {
  getMediaInfo: () => ({
    title: 'Test Song Title',
    artist: 'Test Artist',
    album: 'Test Album',
    playing: true,
    position: 45,
    duration: 180
  }),
  sendMediaKey: (action) => {
    console.log('[MockSMTC] Media key action:', action);
    return true;
  }
};
